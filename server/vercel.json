{
    "installCommand": "npm install --legacy-peer-deps",
    "version": 2,
    "builds":[
        {
            "src": "*.js",
            "use": "@vercel/node"
        },
    ],
    "routes": [
        { "src": "/(.*)", "dest": "/" }
    ]
}
